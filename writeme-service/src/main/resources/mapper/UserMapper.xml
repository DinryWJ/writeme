<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zust.writeme.dao.UserMapper" >
  <resultMap id="BaseResultMap" type="com.zust.writeme.model.User" >
    <!--
      WARNING - @mbg.generated
    -->
    <id column="user_id" property="userId" jdbcType="INTEGER" />
    <id column="user_account" property="userAccount" jdbcType="VARCHAR" />
    <result column="user_password" property="userPassword" jdbcType="VARCHAR" />
    <result column="sex" property="sex" jdbcType="VARCHAR" />
    <result column="user_abstract" property="userAbstract" jdbcType="VARCHAR" />
    <result column="user_permission" property="userPermission" jdbcType="INTEGER" />
    <result column="user_name" property="userName" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="user_base_list">
                user_id,user_name,user_password,user_permission,user_accout,user_abstract
        </sql>

  <select id="selectByAccount" parameterType="String" resultMap="UserResultMapper">
              select * from user as u where u.user_account=#{userAccount,jdbcType=VARCHAR}
        </select>

  <select id="selectAll" resultMap="UserResultMapper" >
        select * from user
    </select>
  <select id="selectByUserName" parameterType="String" resultMap="UserResultMapper" >
        select * from user as u where u.user_name=#{userName,jdbcType=VARCHAR}
    </select>
  <delete id="deleteUser" parameterType="int" >
        delete from user  where user.user_id=#{userId,jdbcType=INTEGER}
    </delete>
  <update id="updatePassword" parameterType="int" >
      update user  set user_password=#{userPassword,jdbcType=VARCHAR} where user_id=#{userId,jdbcType=INTEGER}
    </update>
  <update id="updateUserName" parameterType="int" >
       update user  set user_name=#{userName ,jdbcType=VARCHAR} where user_id=#{userId,jdbcType=INTEGER}
    </update>
</mapper>